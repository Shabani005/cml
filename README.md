Simple "Machine Learning" framework written in C. (Also has python bindings that do not work on windows -> MSVC is bad).
Only ships with KNN and Simple Linear Regression for now.

Build Examples:
```bash
make -B
```


   
